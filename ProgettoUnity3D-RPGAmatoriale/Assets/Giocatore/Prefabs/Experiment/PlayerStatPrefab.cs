using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerStatPrefab : MonoBehaviour
{
    public PlayerStats charStats;
    public SkillMatch charSkillMatch;
    public ClassesMatch charClassMatch;
    public int Numeropg;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
